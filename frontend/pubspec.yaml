name: careerpath
description: Career guidance platform for secondary students.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.10.0
  flutter_riverpod: ^3.3.2
  go_router: ^17.3.0
  shared_preferences: ^2.2.0
  jwt_decoder: ^2.0.1
  fl_chart: ^0.63.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true